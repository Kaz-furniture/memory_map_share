<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kaz_furniture.memoryMapShare">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MemoryMapShareApplication"
        android:allowBackup="true"
        android:icon="@mipmap/icon_picture_second"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_picture_second"
        android:supportsRtl="true"
        android:theme="@style/Theme.MemoryMapShare">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.kaz_furniture.memoryMapShare.activity.MapsActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.CreateMarkerActivity"
            android:theme="@style/Theme.AppCompat"
            android:parentActivityName=".activity.MapsActivity"/>
        <activity android:name=".activity.LoginActivity"
            android:theme="@style/Theme.AppCompat"
            android:parentActivityName=".activity.MapsActivity"/>
        <activity android:name=".activity.CreateAccountActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".activity.PasswordForgetActivity"
            android:theme="@style/Theme.AppCompat"/>
        <activity android:name=".activity.MyPageActivity"
            android:parentActivityName=".activity.MapsActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity android:name=".activity.ProfileEditActivity"
            android:theme="@style/Theme.AppCompat"
            android:parentActivityName=".activity.MyPageActivity"/>
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar"/>
        <activity android:name=".activity.FriendSearchActivity"
            android:theme="@style/Theme.AppCompat"
            android:parentActivityName=".activity.MapsActivity"/>
        <activity android:name=".activity.CreateGroupActivity"
            android:theme="@style/Theme.AppCompat"
            android:windowSoftInputMode="adjustPan"
            android:parentActivityName=".activity.MapsActivity"/>
        <activity
            android:name=".activity.AlbumActivity"
            android:parentActivityName=".activity.MyPageActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"/>

        <activity android:name=".activity.EditGroupActivity"
            android:theme="@style/Theme.AppCompat"
            android:windowSoftInputMode="adjustPan"
            android:parentActivityName=".activity.MapsActivity"/>
        <activity android:name=".activity.EditMarkerActivity"
            android:theme="@style/Theme.AppCompat"
            android:parentActivityName=".activity.AlbumActivity"/>
        <service android:name=".MyFirebaseMessagingService"
            android:exported="false"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>

</manifest>