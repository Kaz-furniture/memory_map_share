<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="markerForImage"
            type="com.kaz_furniture.memoryMapShare.data.Marker" />

    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:background="#cccccc"
        android:layout_width="212dp"
        android:layout_height="180dp"
        android:paddingBottom="4dp">
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:id="@+id/location_name"
            android:maxLines="1"
            android:textSize="16sp"
            android:ellipsize="end"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            tools:text="@string/dateSelect"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/date_display"
            android:textSize="12sp"
            android:layout_marginEnd="4dp"
            tools:text="@string/date"
            app:layout_constraintTop_toBottomOf="@id/location_name"
            app:layout_constraintEnd_toEndOf="parent"/>

        <ImageView
            android:layout_width="100dp"
            android:layout_height="100dp"
            app:layout_constraintTop_toBottomOf="@id/date_display"
            app:layout_constraintStart_toStartOf="parent"
            android:scaleType="centerCrop"
            app:exampleImageFirst="@{markerForImage}"
            android:layout_margin="4dp"
            tools:src="@drawable/plane_picture"
            android:id="@+id/imageView"/>

        <ImageView
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:id="@+id/image_view_second"
            android:scaleType="centerCrop"
            app:exampleImageSecond="@{markerForImage}"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            tools:src="@drawable/plane_picture"
            app:layout_constraintTop_toTopOf="@id/imageView"
            app:layout_constraintEnd_toEndOf="parent"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:id="@+id/textView"
            android:textColor="#000000"
            tools:text="@string/dateSelect"
            android:layout_margin="4dp"
            app:layout_constraintTop_toBottomOf="@id/imageView"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>